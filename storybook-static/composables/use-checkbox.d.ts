import { HCheckboxModel, HCheckboxStatus } from '../manage/types/components/checkbox';
export declare function getCheckboxStatus(modelValue: HCheckboxModel | undefined, propValue: number | string): HCheckboxStatus;
